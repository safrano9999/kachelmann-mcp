# Source of truth: SCRIPTS/githubactions. Generated copies are overwritten.
name: KACHELMANN MCP container image
run-name: ${{ inputs.monitor_id != '' && format('{0} [monitor:{1}:{2}]', github.workflow, inputs.monitor_id, inputs.monitor_final) || github.workflow }}

on:
  workflow_dispatch:
    inputs:
      image_tag:
        description: "Image tag to publish. Empty uses YYYY.M.D."
        required: false
        default: ""
      push_latest:
        description: "Also publish :latest"
        required: true
        type: boolean
        default: true
      monitor_id:
        description: "Optional event-monitor correlation id"
        required: false
        default: ""
        type: string
      monitor_final:
        description: "This run is the final monitored cascade stage"
        required: true
        default: true
        type: boolean

concurrency:
  group: kachelmann-mcp-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: read
  packages: write

jobs:
  build-test-publish:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      IMAGE_NAME: ghcr.io/safrano9999/kachelmann-mcp
      KACHELMANN_REPOSITORY: safrano9999/KACHELMANN
      KACHELMANN_REVISION: c612f483eccab2e032cbaa290bb4ddf7b8c8c082
      DOCKER_BUILD_RECORD_UPLOAD: "false"
    steps:
      - name: Check out container repository
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
        with:
          path: container
          persist-credentials: false

      - name: Check out immutable KACHELMANN source
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
        with:
          repository: ${{ env.KACHELMANN_REPOSITORY }}
          ref: ${{ env.KACHELMANN_REVISION }}
          path: source
          persist-credentials: false
          token: ${{ secrets.KACHELMANN_REPO_TOKEN }}

      - name: Resolve image tags
        id: tags
        shell: bash
        run: |
          set -euo pipefail
          version="${{ inputs.image_tag }}"
          if [ -z "$version" ]; then
            version="$(date -u +%Y.%-m.%-d)"
          fi
          [[ "$version" =~ ^[A-Za-z0-9_.-]+$ ]]
          {
            echo "version=$version"
            echo "tags<<EOF"
            echo "${IMAGE_NAME}:${version}"
            if [ "${{ inputs.push_latest }}" = true ]; then
              echo "${IMAGE_NAME}:latest"
            fi
            echo "EOF"
          } >> "$GITHUB_OUTPUT"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        with:
          cache-binary: false
          keep-state: false
          cleanup: true

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ github.token }}

      - name: Build and publish
        uses: docker/build-push-action@v6
        with:
          context: ./source
          file: ./container/Containerfile
          build-args: |
            KACHELMANN_SOURCE_REVISION=${{ env.KACHELMANN_REVISION }}
          platforms: linux/amd64
          pull: true
          no-cache: true
          push: true
          tags: ${{ steps.tags.outputs.tags }}

      - name: Smoke-test published image
        env:
          IMAGE_TAG: ${{ steps.tags.outputs.version }}
        shell: bash
        run: |
          set -euo pipefail
          image="${IMAGE_NAME}:${IMAGE_TAG}"
          docker pull "$image"
          docker run --rm \
            -e KACHELMANN_EDITOR_TOKEN=ci-editor-token \
            --entrypoint python "$image" -c \
            'from kachelmann.mcp_server import configured_editor_token; assert configured_editor_token() == "ci-editor-token"'
          docker run --rm --entrypoint python "$image" -c \
            'import psycopg; import sqlalchemy; import kachelmann.mcp_server'

          container_id="$(docker run -d \
            -e KACHELMANN_DB_BACKEND=sqlite \
            -e KACHELMANN_EDITOR_TOKEN=ci-editor-token \
            -e KACHELMANN_MCP_ENABLED=true \
            -e KACHELMANN_MCP_PORT=8005 \
            "$image")"
          cleanup() {
            docker container rm --force "$container_id" >/dev/null 2>&1 || true
          }
          trap cleanup EXIT
          for attempt in $(seq 1 30); do
            if [ "$(docker inspect --format '{{.State.Running}}' "$container_id")" != true ]; then
              docker logs "$container_id"
              exit 1
            fi
            if docker exec "$container_id" python -c \
              'from urllib.request import urlopen; urlopen("http://127.0.0.1:8005/", timeout=2).read()'; then
              exit 0
            fi
            sleep 1
          done
          docker logs "$container_id"
          exit 1
